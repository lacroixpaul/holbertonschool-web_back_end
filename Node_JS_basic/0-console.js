function displayMessage(message) {
	console.log(message);
}
module.exports = displayMessage;
// The above code defines a function `displayMessage` that takes a message as an argument and logs it to the console.
